{
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npx prisma generate && npx prisma migrate deploy",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.js"
    }
  ],
  "outputDirectory": "."
}